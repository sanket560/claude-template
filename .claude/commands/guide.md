---
description: Print a plain-language cheat sheet of everything in the shared Claude workspace.
argument-hint: optional area or workflow to focus on
---

Read the root `CLAUDE.md` plus the files in `.claude/standards/`, `.claude/skills/`, `.claude/agents/`, and `.claude/commands/`. Summarize the repo rules in plain language, highlight the practical gotchas, and point the user to the most relevant command or skill for the task they are starting.
